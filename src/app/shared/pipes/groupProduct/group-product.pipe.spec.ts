import { GroupProductPipe } from "./group-product.pipe";

describe("GroupProductPipe", () => {
  it("create an instance", () => {
    const pipe = new GroupProductPipe();
    expect(pipe).toBeTruthy();
  });
});
